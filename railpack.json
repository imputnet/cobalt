{
  "install": {
    "cmd": "corepack enable && corepack prepare pnpm@latest --activate && pnpm install"
  },
  "build": {
    "cmd": "echo build done"
  },
  "start": {
    "cmd": "cd api && pnpm start"
  }
}
